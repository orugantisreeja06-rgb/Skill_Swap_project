# Loaded automatically when you run: npm start
# Point this at your Express API (see server/.env PORT — default 5000).
# No /api suffix — the app appends /api for REST calls.
REACT_APP_API_URL=http://localhost:5000
